import {extendTheme} from "@chakra-ui/theme-utils";

const theme = extendTheme(
    {
        colors: {
            faintPink: {
                50: "#fde7fc"
            }
        }
    }
)

export default theme;